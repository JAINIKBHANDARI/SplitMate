import { useState } from 'react';
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { api, ApiError } from '../../lib/api';
import { Button, Modal } from '../../components/ui';
export function SettlementDialog({ group, members, open, onClose, from, to, amountMinor }) { const people = members.filter((member) => member.status === 'active' && member.userId).map((member) => ({ id: member.userId._id ?? member.userId.id, name: member.userId.name })); const [error, setError] = useState(''); const client = useQueryClient(); const mutation = useMutation({ mutationFn: (body) => api.post(`/groups/${group._id}/settlements`, body), onSuccess: () => { ['balances', 'group', 'dashboard', 'settlements'].forEach((key) => client.invalidateQueries({ queryKey: [key, group._id] })); onClose(); }, onError: (issue) => setError(issue instanceof ApiError ? issue.message : 'Could not record settlement.') }); return <Modal open={open} onClose={onClose} title="Record settlement"><form className="space-y-4" onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); const fromUserId = String(form.get('fromUserId')), toUserId = String(form.get('toUserId')); if (fromUserId === toUserId)
    return setError('Choose two different members.'); mutation.mutate({ fromUserId, toUserId, amountMinor: Math.round(Number(form.get('amount')) * 100), method: form.get('method'), note: form.get('note'), status: 'completed' }); }}><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-sm font-semibold">From</span><select name="fromUserId" defaultValue={from} className="field">{people.map((person) => <option key={person.id} value={person.id}>{person.name}</option>)}</select></label><label><span className="mb-1.5 block text-sm font-semibold">To</span><select name="toUserId" defaultValue={to ?? people[1]?.id} className="field">{people.map((person) => <option key={person.id} value={person.id}>{person.name}</option>)}</select></label></div><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-sm font-semibold">Amount</span><input required name="amount" min="0.01" step="0.01" defaultValue={amountMinor ? amountMinor / 100 : ''} className="field" type="number" inputMode="decimal"/></label><label><span className="mb-1.5 block text-sm font-semibold">Method</span><select name="method" className="field"><option value="upi">UPI</option><option value="cash">Cash</option><option value="bank">Bank transfer</option><option value="card">Card</option><option value="other">Other</option></select></label></div><label className="block"><span className="mb-1.5 block text-sm font-semibold">Note <small className="font-normal text-slate-400">Optional</small></span><input name="note" className="field" placeholder="Paid in full"/></label>{error && <p className="text-sm text-rose-600">{error}</p>}<div className="flex justify-end gap-2 pt-2"><Button type="button" variant="ghost" onClick={onClose}>Cancel</Button><Button loading={mutation.isPending}>Record payment</Button></div></form></Modal>; }
