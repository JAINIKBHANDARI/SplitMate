import { ArrowLeft } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
import { MarketingFooter } from '../components/MarketingFooter';
const privacy = { eyebrow: 'Privacy', title: 'Your data, treated with care.', updated: 'Updated June 2026', sections: [['What we store', 'Your profile details, groups, expenses, balances, settlements, and session information needed to run SplitMate.'], ['How we use it', 'Only to provide the shared-expense service: calculating balances, showing activity, and keeping your account secure.'], ['Security', 'Passwords are hashed. Sessions use secure HttpOnly cookies. We do not store payment card information.'], ['Your choices', 'You can request account or data help by emailing hello@splitmate.app.']] };
const terms = { eyebrow: 'Terms', title: 'Simple rules for a shared space.', updated: 'Updated June 2026', sections: [['Using SplitMate', 'Use accurate information and only add people to groups you are allowed to manage.'], ['Your responsibility', 'Review expenses and settlements before recording them. SplitMate helps calculate shared balances; it does not move money.'], ['Availability', 'We work to keep the service available and secure, but please keep your own important records as well.'], ['Questions', 'For support or legal questions, contact hello@splitmate.app.']] };
export default function Legal() { const isPrivacy = useLocation().pathname === '/privacy'; const page = isPrivacy ? privacy : terms; return <div className="min-h-screen bg-paper"><main className="mx-auto max-w-3xl px-5 pb-20 pt-16 sm:pt-24"><Link to="/" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500 transition hover:text-violet"><ArrowLeft className="size-4"/>Back to SplitMate</Link><p className="eyebrow mt-12">{page.eyebrow}</p><h1 className="mt-3 text-4xl font-extrabold tracking-[-.05em] sm:text-5xl">{page.title}</h1><p className="mt-4 text-sm text-slate-500">{page.updated}</p><div className="mt-12 space-y-9">{page.sections.map(([heading, body]) => <section key={heading}><h2 className="text-lg font-bold tracking-tight">{heading}</h2><p className="mt-2 max-w-2xl text-sm leading-7 text-slate-500">{body}</p></section>)}</div></main><MarketingFooter /></div>; }
