import { useQuery } from '@tanstack/react-query';
import { CreditCard } from 'lucide-react';
import { api } from '../lib/api';
import { dateLabel, money } from '../lib/format';
import { Card, Empty, Skeleton } from '../components/ui';
export default function Settlements() { const groups = useQuery({ queryKey: ['groups'], queryFn: () => api.get('/groups') }); const settlements = useQuery({ queryKey: ['settlements'], enabled: !!groups.data, queryFn: async () => { const rows = await Promise.all((groups.data?.groups ?? []).map(async (group) => ({ group, settlements: (await api.get(`/groups/${group._id}/settlements`)).settlements }))); return rows.flatMap((row) => row.settlements.map((settlement) => ({ ...settlement, group: row.group }))); } }); return <div className="space-y-6"><section><p className="eyebrow">Payment history</p><h1 className="mt-1 text-3xl font-extrabold tracking-tight">Settlements.</h1></section>{groups.isLoading || settlements.isLoading ? <Skeleton className="h-64"/> : !settlements.data?.length ? <Empty icon={<CreditCard />} title="No settlements yet"/> : <Card className="overflow-hidden">{settlements.data.map((item) => <div key={item._id} className="flex items-center gap-3 border-b px-5 py-4 last:border-0"><span className="grid size-10 place-items-center rounded-xl bg-mint/10 text-mint"><CreditCard className="size-5"/></span><span className="min-w-0 flex-1"><b className="block text-sm">{item.fromUserId?.name} paid {item.toUserId?.name}</b><small className="text-xs text-slate-500">{item.group.name} · {dateLabel(item.settledAt)}</small></span><span className="text-right"><b className="block text-sm">{money(item.amountMinor, item.currency)}</b><small className={`text-xs ${item.status === 'cancelled' ? 'text-rose-500' : 'text-mint'}`}>{item.status}</small></span></div>)}</Card>}</div>; }
