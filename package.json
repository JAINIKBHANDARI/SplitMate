{
  "name": "splitmate",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run build -w @splitmate/shared && concurrently -n api,web -c cyan,magenta \"npm run dev -w @splitmate/api\" \"npm run dev -w @splitmate/web\"",
    "build": "npm run build -w @splitmate/shared && npm run build -w @splitmate/web && npm run build -w @splitmate/api",
    "start": "npm run start -w @splitmate/api",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "npm run test:e2e -w @splitmate/web",
    "seed": "npm run seed -w @splitmate/api",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.5.2"
  },
  "engines": { "node": ">=20.11" }
}
